apiVersion: v1
kind: Service

metadata:
  name: nodejs-devops-service

spec:
  selector:
    app: nodejs-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: NodePort